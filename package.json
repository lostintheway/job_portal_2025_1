{
  "name": "photography_web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter admin dev\" \"pnpm --filter server start\"",
    "tsc": "tsc --init",
    "db:generate": "pnpm --filter server run db:generate",
    "db:migrate": "pnpm --filter server run db:migrate",
    "db:push": "pnpm --filter server run db:push"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "concurrently": "^9.0.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@hookform/resolvers": "^3.9.0",
    "axios": "^1.7.7",
    "react-hook-form": "^7.53.0",
    "react-intersection-observer": "^9.13.1",
    "zod": "^3.23.8"
  }
}
